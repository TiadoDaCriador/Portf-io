export interface Project {
  title: string;
  category: string;      // ex: "App mobile" | "Web app"
  description: string;
  result: string;
  tags: string[];
  repoUrl: string;        // substituir por link real do repositório/demo
}
